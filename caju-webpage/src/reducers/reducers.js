const { ADD_ITEM, REMOVE_ITEM } = require("../actions/actions");

export const counter = (state, action) => {
    switch (action.type) {
        case ADD_ITEM:
        case REMOVE_ITEM:
        default:
    }
};
